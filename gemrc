# Prevent generating the ri and RDoc documentation.
install: --no-rdoc --no-ri
update: --no-rdoc --no-ri
# If you want to overwrite this default per gem, or if you want to install the documentation later,
# you can do so by passing --ri and/or --rdoc options to the gem install command.
